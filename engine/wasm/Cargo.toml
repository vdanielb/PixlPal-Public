[package]
name = "pixelcam-engine-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "wasm-bindgen bindings for the PixlPal engine (web frontend)."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pixelcam-engine = { path = "../core" }
serde_json = "1"
wasm-bindgen = "0.2"
