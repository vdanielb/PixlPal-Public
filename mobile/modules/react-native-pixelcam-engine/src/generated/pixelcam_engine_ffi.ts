// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
/* tslint:disable */
/* eslint-disable */
// @ts-nocheck
import nativeModule from "./pixelcam_engine_ffi-ffi";
import { type UniffiRustFutureContinuationCallback, type UniffiForeignFutureDroppedCallback, type UniffiForeignFutureDroppedCallbackStruct,
} from "./pixelcam_engine_ffi-ffi";
import { type UniffiByteArray, AbstractFfiConverterByteArray, FfiConverterArrayBuffer, FfiConverterInt32, FfiConverterOptional, FfiConverterUInt32, FfiConverterUInt8, RustBuffer, UniffiError, UniffiInternalError, UniffiRustCaller, uniffiCreateFfiConverterString, uniffiTypeNameSymbol, variantOrdinalSymbol,
} from "@ubjs/core";
const uniffiCaller = new UniffiRustCaller(() => ({ code: 0 }));

const uniffiIsDebug =
  // @ts-ignore -- The process global might not be defined
  typeof process !== "object" ||
  // @ts-ignore -- The process global might not be defined
  process?.env?.NODE_ENV !== "production" ||
  false;

// Public interface members begin here.

/**
 * Process encoded image bytes (JPEG/PNG/WebP) and re-encode.
 * `format` is "png" or "jpeg"; `quality` applies to JPEG only (1-100).
 */
export function processEncoded(imageBytes: ArrayBuffer, pipelineJson: string, format: string, quality: number): ArrayBuffer /*throws*/ {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterArrayBuffer.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeEngineError.lift.bind(FfiConverterTypeEngineError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_pixelcam_engine_ffi_fn_func_process_encoded(
        FfiConverterArrayBuffer.lower(imageBytes, nativeModule().rustbuffer_alloc),
        FfiConverterString.lower(pipelineJson, nativeModule().rustbuffer_alloc),
        FfiConverterString.lower(format, nativeModule().rustbuffer_alloc),
        FfiConverterUInt8.lower(quality, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

/**
 * Process a raw RGBA8 pixel buffer through a pipeline JSON document.
 */
export function processRgba8(pixels: ArrayBuffer, width: number, height: number, pipelineJson: string): ArrayBuffer /*throws*/ {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterArrayBuffer.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCallWithError(
            /*liftError:*/ FfiConverterTypeEngineError.lift.bind(FfiConverterTypeEngineError),
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_pixelcam_engine_ffi_fn_func_process_rgba8(
        FfiConverterArrayBuffer.lower(pixels, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(width, nativeModule().rustbuffer_alloc),
        FfiConverterUInt32.lower(height, nativeModule().rustbuffer_alloc),
        FfiConverterString.lower(pipelineJson, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

/**
 * Validate a pipeline JSON document. Returns an error message, or None when valid.
 */
export function validatePipeline(pipelineJson: string): string | undefined {
    return ((__rb: Uint8Array) => {
        try {
            return FfiConverterOptionalString.lift(__rb);
        } finally {
            nativeModule().rustbuffer_free(__rb);
        }
    })(uniffiCaller.rustCall(
            /*caller:*/ (callStatus) => {
                return nativeModule().ubrn_uniffi_pixelcam_engine_ffi_fn_func_validate_pipeline(
        FfiConverterString.lower(pipelineJson, nativeModule().rustbuffer_alloc),
                callStatus);
            },
            /*liftString:*/ FfiConverterString.lift.bind(FfiConverterString),
    ));
    }

// Hermes (React Native ≥ 0.74) ships TextEncoder and encodeInto, but not
// TextDecoder. For single-string decode (bytesToString), we polyfill via the
// C++ string_from_buffer helper using a duck-typed object matching the
// standard TextDecoder.decode signature. Once Hermes ships a real
// TextDecoder, the `typeof` check will pick it up automatically.
//
// For array-of-strings decode (readStringFromBuffer), we keep a dedicated C++
// helper: the polyfill path (new Uint8Array view + decode) measured ~40%
// slower on getStringArray benchmarks than a direct (buf, offset, length)
// call, due to the per-read view allocation and extra property lookups in
// string_from_buffer.
const stringConverter = (() => {
    const encoder = new TextEncoder();
    const decoder: { decode(input: UniffiByteArray): string } =
        typeof TextDecoder !== "undefined"
            ? new TextDecoder()
            : {
                  decode: (bytes: UniffiByteArray) =>
                      nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_from_buffer(
                          bytes,
                          undefined as any,
                      ) as string,
              };
    return {
        // Single-string lower() uses the C++ helper — TextEncoder.encode
        // measured ~43% slower on takeString benchmarks.
        stringToBytes: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_buffer(s, undefined as any),
        bytesToString: (ab: UniffiByteArray) => decoder.decode(ab),
        // Direct C++ call — bypasses uniffiCaller.rustCall() overhead.
        // Matters for N-element arrays.
        stringByteLength: (s: string) =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length(s, undefined as any) as number,
        // Encode directly into the RustBuffer backing store via
        // TextEncoder.encodeInto — zero intermediate allocation. Replaces
        // the old C++ write_string_into_buffer helper.
        writeStringIntoBuffer: (s: string, buf: any, offset: number): number => {
            const view = new Uint8Array(
                buf.arrayBuffer,
                offset,
                buf.arrayBuffer.byteLength - offset,
            );
            return encoder.encodeInto(s, view).written;
        },
        // Dedicated C++ helper — avoids per-read Uint8Array allocation and
        // the double property-lookup in string_from_buffer.
        readStringFromBuffer: (buf: any, offset: number, length: number): string =>
            nativeModule().ubrn_uniffi_internal_fn_func_ffi__read_string_from_buffer(buf, offset, length) as string,
    };
})();
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter);


// Error type: EngineError
export enum EngineError_Tags {
    ImageDecode = "ImageDecode",
    ImageEncode = "ImageEncode",
    InvalidPipeline = "InvalidPipeline",
    UnsupportedVersion = "UnsupportedVersion",
    UnknownOperation = "UnknownOperation"
}
export const EngineError = (() => {

    type ImageDecode__interface = {
        tag: EngineError_Tags.ImageDecode;
        inner: 
Readonly<{message: string}>
    };
    class ImageDecode_ extends UniffiError implements ImageDecode__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "EngineError";
        readonly tag = EngineError_Tags.ImageDecode;
        readonly inner: 
Readonly<{message: string}>;
        constructor(
inner: {message: string }) {
            super("EngineError", "ImageDecode");

            this.inner = Object.freeze(inner);
        }
        static new(
inner: {message: string }): ImageDecode_ {
            return new ImageDecode_(inner);
        }

        static instanceOf(obj: any): obj is ImageDecode_ {
            return obj.tag === EngineError_Tags.ImageDecode;
        }
        static hasInner(obj: any): obj is ImageDecode_ {
            return ImageDecode_.instanceOf(obj);
        }

        static getInner(obj: ImageDecode_): 
Readonly<{message: string}> {
            return obj.inner;
        }

    }

    type ImageEncode__interface = {
        tag: EngineError_Tags.ImageEncode;
        inner: 
Readonly<{message: string}>
    };
    class ImageEncode_ extends UniffiError implements ImageEncode__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "EngineError";
        readonly tag = EngineError_Tags.ImageEncode;
        readonly inner: 
Readonly<{message: string}>;
        constructor(
inner: {message: string }) {
            super("EngineError", "ImageEncode");

            this.inner = Object.freeze(inner);
        }
        static new(
inner: {message: string }): ImageEncode_ {
            return new ImageEncode_(inner);
        }

        static instanceOf(obj: any): obj is ImageEncode_ {
            return obj.tag === EngineError_Tags.ImageEncode;
        }
        static hasInner(obj: any): obj is ImageEncode_ {
            return ImageEncode_.instanceOf(obj);
        }

        static getInner(obj: ImageEncode_): 
Readonly<{message: string}> {
            return obj.inner;
        }

    }

    type InvalidPipeline__interface = {
        tag: EngineError_Tags.InvalidPipeline;
        inner: 
Readonly<{message: string}>
    };
    class InvalidPipeline_ extends UniffiError implements InvalidPipeline__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "EngineError";
        readonly tag = EngineError_Tags.InvalidPipeline;
        readonly inner: 
Readonly<{message: string}>;
        constructor(
inner: {message: string }) {
            super("EngineError", "InvalidPipeline");

            this.inner = Object.freeze(inner);
        }
        static new(
inner: {message: string }): InvalidPipeline_ {
            return new InvalidPipeline_(inner);
        }

        static instanceOf(obj: any): obj is InvalidPipeline_ {
            return obj.tag === EngineError_Tags.InvalidPipeline;
        }
        static hasInner(obj: any): obj is InvalidPipeline_ {
            return InvalidPipeline_.instanceOf(obj);
        }

        static getInner(obj: InvalidPipeline_): 
Readonly<{message: string}> {
            return obj.inner;
        }

    }

    type UnsupportedVersion__interface = {
        tag: EngineError_Tags.UnsupportedVersion;
        inner: 
Readonly<{version: number}>
    };
    class UnsupportedVersion_ extends UniffiError implements UnsupportedVersion__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "EngineError";
        readonly tag = EngineError_Tags.UnsupportedVersion;
        readonly inner: 
Readonly<{version: number}>;
        constructor(
inner: {version: number }) {
            super("EngineError", "UnsupportedVersion");

            this.inner = Object.freeze(inner);
        }
        static new(
inner: {version: number }): UnsupportedVersion_ {
            return new UnsupportedVersion_(inner);
        }

        static instanceOf(obj: any): obj is UnsupportedVersion_ {
            return obj.tag === EngineError_Tags.UnsupportedVersion;
        }
        static hasInner(obj: any): obj is UnsupportedVersion_ {
            return UnsupportedVersion_.instanceOf(obj);
        }

        static getInner(obj: UnsupportedVersion_): 
Readonly<{version: number}> {
            return obj.inner;
        }

    }

    type UnknownOperation__interface = {
        tag: EngineError_Tags.UnknownOperation;
        inner: 
Readonly<{op: string}>
    };
    class UnknownOperation_ extends UniffiError implements UnknownOperation__interface {
        /**
         * @private
         * This field is private and should not be used, use `tag` instead.
         */
        readonly [uniffiTypeNameSymbol] = "EngineError";
        readonly tag = EngineError_Tags.UnknownOperation;
        readonly inner: 
Readonly<{op: string}>;
        constructor(
inner: {op: string }) {
            super("EngineError", "UnknownOperation");

            this.inner = Object.freeze(inner);
        }
        static new(
inner: {op: string }): UnknownOperation_ {
            return new UnknownOperation_(inner);
        }

        static instanceOf(obj: any): obj is UnknownOperation_ {
            return obj.tag === EngineError_Tags.UnknownOperation;
        }
        static hasInner(obj: any): obj is UnknownOperation_ {
            return UnknownOperation_.instanceOf(obj);
        }

        static getInner(obj: UnknownOperation_): 
Readonly<{op: string}> {
            return obj.inner;
        }

    }

    function instanceOf(obj: any): obj is EngineError {
        return obj[uniffiTypeNameSymbol] === "EngineError";
    }

    return Object.freeze({
        instanceOf,
  ImageDecode: ImageDecode_, 
  ImageEncode: ImageEncode_, 
  InvalidPipeline: InvalidPipeline_, 
  UnsupportedVersion: UnsupportedVersion_, 
  UnknownOperation: UnknownOperation_
    });

})();
export type EngineError = InstanceType<
    typeof EngineError['ImageDecode' | 'ImageEncode' | 'InvalidPipeline' | 'UnsupportedVersion' | 'UnknownOperation']
>;

// FfiConverter for enum EngineError
const FfiConverterTypeEngineError = (() => {
    const ordinalConverter = FfiConverterInt32;
    type TypeName = EngineError;
    class FFIConverter extends AbstractFfiConverterByteArray<TypeName> {
        read(from: RustBuffer): TypeName {
            switch (ordinalConverter.read(from)) {
                case 1: return new EngineError.ImageDecode({message: FfiConverterString.read(from) });
                case 2: return new EngineError.ImageEncode({message: FfiConverterString.read(from) });
                case 3: return new EngineError.InvalidPipeline({message: FfiConverterString.read(from) });
                case 4: return new EngineError.UnsupportedVersion({version: FfiConverterUInt32.read(from) });
                case 5: return new EngineError.UnknownOperation({op: FfiConverterString.read(from) });
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        write(value: TypeName, into: RustBuffer): void {
            switch (value.tag) {
                case EngineError_Tags.ImageDecode: {
                    ordinalConverter.write(1, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner.message, into);
                    return;
                }
                case EngineError_Tags.ImageEncode: {
                    ordinalConverter.write(2, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner.message, into);
                    return;
                }
                case EngineError_Tags.InvalidPipeline: {
                    ordinalConverter.write(3, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner.message, into);
                    return;
                }
                case EngineError_Tags.UnsupportedVersion: {
                    ordinalConverter.write(4, into);
                    const inner = value.inner;
                    FfiConverterUInt32.write(inner.version, into);
                    return;
                }
                case EngineError_Tags.UnknownOperation: {
                    ordinalConverter.write(5, into);
                    const inner = value.inner;
                    FfiConverterString.write(inner.op, into);
                    return;
                }
                default:
                    // Throwing from here means that EngineError_Tags hasn't matched an ordinal.
                    throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
        allocationSize(value: TypeName): number {
            switch (value.tag) {
                case EngineError_Tags.ImageDecode: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(1);
                    size += FfiConverterString.allocationSize(inner.message);
                    return size;
                }
                case EngineError_Tags.ImageEncode: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(2);
                    size += FfiConverterString.allocationSize(inner.message);
                    return size;
                }
                case EngineError_Tags.InvalidPipeline: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(3);
                    size += FfiConverterString.allocationSize(inner.message);
                    return size;
                }
                case EngineError_Tags.UnsupportedVersion: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(4);
                    size += FfiConverterUInt32.allocationSize(inner.version);
                    return size;
                }
                case EngineError_Tags.UnknownOperation: {
                    const inner = value.inner;
                    let size = ordinalConverter.allocationSize(5);
                    size += FfiConverterString.allocationSize(inner.op);
                    return size;
                }
                default: throw new UniffiInternalError.UnexpectedEnumCase();
            }
        }
    }
    return new FFIConverter();
})();

// FfiConverter for string | undefined
const FfiConverterOptionalString = new FfiConverterOptional(FfiConverterString);


/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
    // Get the bindings contract version from our ComponentInterface
    const bindingsContractVersion = 30;
    // Get the scaffolding contract version by calling the into the dylib
    const scaffoldingContractVersion = nativeModule().ubrn_ffi_pixelcam_engine_ffi_uniffi_contract_version();
    if (bindingsContractVersion !== scaffoldingContractVersion) {
        throw new UniffiInternalError.ContractVersionMismatch(scaffoldingContractVersion, bindingsContractVersion);
    }
    if (nativeModule().ubrn_uniffi_pixelcam_engine_ffi_checksum_func_process_encoded() !== 8123) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_pixelcam_engine_ffi_checksum_func_process_encoded");
    }
    if (nativeModule().ubrn_uniffi_pixelcam_engine_ffi_checksum_func_process_rgba8() !== 50756) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_pixelcam_engine_ffi_checksum_func_process_rgba8");
    }
    if (nativeModule().ubrn_uniffi_pixelcam_engine_ffi_checksum_func_validate_pipeline() !== 21583) {
        throw new UniffiInternalError.ApiChecksumMismatch("uniffi_pixelcam_engine_ffi_checksum_func_validate_pipeline");
    }

    }

export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeEngineError,
  }
});