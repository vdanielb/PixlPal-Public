{
  "name": "@pixelcam/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Pipeline schema, TypeScript types, operation metadata, and built-in presets for the PixlPal editor.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./pipeline.schema.json": "./pipeline.schema.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
